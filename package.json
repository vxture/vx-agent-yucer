{
  "name": "yucer",
  "version": "0.0.0",
  "private": true,
  "description": "yucer - enterprise sales super-agent: strategy, planning, execution, accounts, opportunity detection and management, delivery, and a sales copilot across the whole chain.",
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=22"
  },
  "pnpm": {
    "comment": "SCA remediation. Each entry pins a transitive dependency past a known vulnerability that osv-scanner blocks on; the gate is never widened to accommodate one. Raise the bound rather than remove the entry when a newer advisory lands.",
    "overrides": {
      "postcss@<8.5.23": ">=8.5.23",
      "@hono/node-server@<1.19.13": ">=1.19.13",
      "sharp@<0.35.0": ">=0.35.0",
      "nanoid@<3.3.18": ">=3.3.18 <4",
      "fast-uri@<3.1.5": ">=3.1.5 <4",
      "valibot@<1.4.2": ">=1.4.2",
      "hono@<4.12.34": ">=4.12.34"
    }
  },
  "scripts": {
    "build": "pnpm --filter @yucer/app build",
    "type-check:all": "pnpm --filter @yucer/shared type-check",
    "lint": "pnpm --recursive --if-present lint",
    "lint:docs-numbering": "node scripts/guardrails/check-docs-numbering.mjs --strict",
    "lint:data-design": "node scripts/guardrails/check-data-architecture.mjs --strict",
    "lint:ports": "node scripts/guardrails/check-port-consistency.mjs --strict",
    "lint:envelope": "node scripts/guardrails/check-error-envelope.mjs --strict"
  }
}
