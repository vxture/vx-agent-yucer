/**
 * POST /api/webhooks/vxture —— 平台 C3 下发的**标准**接收路径。
 *
 * 《产品接入通则》§C3 下发规定所有产品同一个路径，变的只有域名。本产品此前只挂在
 * `/provisioning/webhook` 上——那个取值不是规范：它和另一个产品各自造了同一个名字，
 * 两边的注释都标着「product_200 section 4」，而那一节通篇只规定义务（HMAC、幂等、
 * 建/拆空间）、从没规定过路径。第三个产品照抄，于是三方都以为自己在遵守规定。
 * 平台侧当时也接不住：登记接口只校验协议与长度、不看路径。2026-09-13 起登记处
 * 当场拒收非标准路径，上线检查第 7 项也跟着判。
 *
 * **这是 X-4 三步迁移的第 ① 步：先同时收两个路径。**
 *   ① 产品侧新旧两路都能收，并上线  ← 本文件
 *   ② 平台侧把登记地址改成这一个
 *   ③ 产品侧撤掉 `app/provisioning/webhook/`，只留本文件
 *
 * 顺序不能换。先改平台登记会有一段投递落空的窗口，而落空的表现不是报错——
 * 未匹配的路径会落到前端 catch-all 返回页面和 200，投递被判为送达。
 * 第 ③ 步不是可选的：留着两条路，下一个人无法从代码判断线上登记的是哪一个。
 *
 * 转出实现而不是复制一份：两条路必须是**同一个处理器**。复制会得到两份各自演进的
 * 验签与幂等逻辑，而它们不一致时，症状取决于平台当天登记的是哪个地址。
 */
export { POST } from "../../../provisioning/webhook/route";

/* 段配置要在本文件里直接声明——Next 的段配置靠静态分析取值，`export { dynamic }
   from …` 这种再导出形式它读不到，读不到时会静默按默认（静态）处理，
   而一个被静态化的 webhook 端点不会执行验签。 */
export const dynamic = "force-dynamic";
