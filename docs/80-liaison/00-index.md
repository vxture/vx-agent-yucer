# 80-liaison - Cross-org liaison

Cross-organization liaison for this repo: reply letters, integration agreements,
and sync notes with other product lines or the platform line. Artifacts carry a
`YYMMDDHHMM` stamp (in the filename after the `NN-` index, so the docs-numbering
guardrail still passes).

Empty - `yucer` has not opened any liaison yet. The template's own correspondence
(its `vxtpl` demo instantiation: edge vhost, credential request, port
reassignment) belongs to the template's history and was deliberately not carried
into this product repo.

The first letters this repo is expected to need are batch 4 (see
`docs/70-workplan/00-index.md`): the platform-side registration of the
`yucer` / `yucer-beta` OIDC clients, the `yucer.vxture.com` edge vhost, and the
app port assignment.

| File | Stamp | To | Subject | Status |
|------|-------|----|---------|--------|
| _(none yet)_ | | | | |
