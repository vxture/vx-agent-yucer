# 70-workplan - 构建计划与批次跟踪

本仓由 `vxture-template` 实例化而来（product_code = `yucer`）。每个批次是一个 PR，
带机器可验收的门禁。治理基座、平台集成契约面、部署流水线随模板继承，**不重做**。

## 批次 0 - 继承自模板（已完成，不在本仓重做）

模板已交付并验证的部分，实例化后直接生效：

| 来源批次 | 范围 | 状态 |
|---------|------|------|
| 模板批次 1（A-D） | 治理基座、四层密钥卫生、SCA 硬门禁、docs 编号体系、分支保护 ruleset | 继承 |
| 模板批次 2（2a-2g） | 平台集成三通道（C1 OIDC RP / C2 权益 / C3 供给与用量）、业务面 DB 契约 schema、离线 Mock 验证页 | 继承 |
| 模板批次 E | 部署流水线（`deploy`/`build`/`rollback`/`db-init` + `tailnet-ssh-connect`），已在模板演示实例上端到端验证 | 继承 |

五个必需检查是稳定契约，**不得重命名**：`quality-gate` / `build` /
`test-coverage` / `audit` / `gitleaks`。

## 批次 1 - 产品域框架（本批次）

把模板的留白区按销售超级智能体填满。交付的是**结构**，不是业务页面实现。

| 项 | 交付物 | 验收 |
|----|-------|------|
| 1a | 产品定义与八个能力分区边界 | `docs/20-specs/10`、`20` |
| 1b | 全链路业务规则（阶段机、预测口径、归因、人机边界） | `docs/20-specs/30` |
| 1c | 能力矩阵（档位 x feature key）+ 实现 + 单测 | `capability.ts` / `capability.test.ts`；`docs/20-specs/40` |
| 1d | 角色权限目录 + 幂等种子 | `incr/0001_seed_authz_catalog.sql`；`docs/20-specs/50` |
| 1e | 域数据模型：5 个域 schema / 24 张表 + 服务角色授权 + 列级写锁 | `00_baseline.sql`、`97`、`98` |
| 1f | Prisma 与 DDL lockstep | `check-data-architecture.mjs --strict` 34 张表全对齐 |
| 1g | 域架构与数据设计文档 + 三条 ADR | `docs/30-design/` |

**批次 1 验收**：`lint:docs-numbering --strict` 与 `lint:data-design --strict` 均
exit 0；五个 CI 检查全绿。

## 批次 2 - 域服务层、两道门、智能体两平面

| 项 | 范围 | 状态 |
|----|------|------|
| 2a | 权限门 `authz/`：成员懒加载、角色权限判定、与权益门的组合判定、动作目录 | 已交付 |
| 2b | 域业务规则纯函数 `domains/<d>/lib/`：阶段机、归因计算、预测汇总、信号评分、提案生命周期（全部可单测、不碰 IO） | 八个能力分区全部已交付 |
| 2c | 域持久化端口 + 服务层 + Prisma 适配器（沿用模板的 port/adapter 形态） | **八个能力分区全部交付**：端口、服务、Prisma 适配器齐备 |
| 2d | **智能体两平面接入**（ADR-004）：`platform/s2s` 令牌、`agent/atlas` 模型面、`agent/runos` 能力面与 Skill 分发 | 已交付 |
| 2e | 智能体编排 `agent/orchestrator`：会话回合把模型输出落成 `agent_action` 提案 | 已交付 |

### 2a/2b/2d 已交付内容

- `authz/`：19 权限 / 7 角色的**类型化镜像**，`catalog.test.ts` 逐条解析
  `incr/0001` 种子 SQL 断言双向一致——镜像漂移会让 CI 红，而不是悄悄分叉。
  两道门的顺序（权益先、权限后）由 `gate.ts` 固化并单测。
- `authz/actions.ts`：产品动作目录，把每个动作与它的 (feature key, 权限码) 配对
  写成**数据**。这是防「某个写接口只查了权益、忘了查权限」的结构性手段。
- `agent/atlas`：**只按 `endpointCode` 路由**，四个 copilot 任务各自可由环境变量改指；
  错误按码分类重试（`GRANT_DENIED` / `QUOTA_EXCEEDED` 永不重试，`RATE_LIMITED` 用
  body 里的延迟而非 `Retry-After` 头）；SSE 中途失败会**抛出**而不是被当成正常结束。
- `agent/runos`：MCP 四工具面，`task_id` 本地先校验；两层错误（传输层 / 工具结果层）
  分别建模。Skill **只分发不执行**：校验 `result_kind`、逐次核对 `content_digest`、
  渲染进提示词时带来源围栏。
- `platform/s2s`：缺 `workspace_id` **直接抛错**——Atlas 缺它不报错，只是静默跳过配额
  并把用量记到 NULL 工作空间。

### 2c 的分层形态（八个能力分区一致）

```
service.ts   两道门 -> 规则纯函数 -> 端口          [八个能力分区全覆盖]
store.ts     端口 + 内存实现（workspace_id 是每个方法的第一个参数）
prisma-*.ts  Prisma 适配器，写入前过列锁镜像       [八个能力分区全覆盖]
```

`domains/shared/column-locks.ts` 是 `98_column_locks.sql` 的类型化镜像，
`column-locks.test.ts` 解析 DDL 双向对账。适配器写库**前**校验补丁，把
`permission denied for column ...` 变成调用点上的具名违规——数据库仍是兜底，
但不再是第一个说不的人。

`pnpm --filter @yucer/app test`：560 项全绿。

## 批次 3 - 产品界面与智能体交互（已交付）

| 项 | 范围 | 状态 |
|----|------|------|
| 3a | 八个能力分区路由与导航，两道门接入页面。UI 元件一律取自 `@vxture/design-system` | 已交付。`routes.test.ts` 断言每个导航项都有页面——导航先于目的地上线过一次，这条守卫防它再来 |
| 3b | 商机管道与预测视图 | 已交付。汇总与快照写入用**同一个** `rollUp()`，读到的和冻结的不可能不一致 |
| 3c | 智能体会话与提案裁决——含批量裁决 | 已交付。会话与队列同页但**视觉分离**，无内联采纳；批量确认由 `batchRisk()` 生成，无法渲染成「采纳 200 项」 |
| 3d | 信号收件箱与处置 | 已交付（超出原计划）。评分、判重、忽略、升级为线索 |
| 3e | 流式回答 | 已交付（超出原计划）。持久化契约与非流式一致，含消费者中途放弃的情形 |
| 3f | 剧本接地与剧本目录 | 已交付（超出原计划）。`agent_playbook` 按主题域选取、**上限 3 条**、仅 active、内容加围栏并标注为参考材料；目录在助手页只读可见——被注入提示词的文本必须可被阅读和质疑 |
| 3g | 全链路演示数据 | 已交付（超出原计划）。5 家客户 / 10 条商机（覆盖全部开放阶段）/ 4 个交付项目 / 8 条信号 / 5 条线索 / 4 条剧本，跨域引用由 `demo-seed.test.ts` 断言，含一条贯穿计划→战役→信号→线索→商机→项目→回款的完整链条 |
| 3h | 商机详情：推进 / 定价 / 阶段轨迹 | 已交付（PR #22）。`advanceStage` 与 `submitForecast` 此前**零非测试调用方**——后端完整而界面从未接上，转化来的商机永远停在 `qualify`、金额为 null、无法关闭。同时让 `planProbabilityOverride` 首次可达：「人工赢率覆盖机器建议」在没人能设置赢率之前只能表达一半 |
| 3i | 成员与角色管理 | 已交付（PR #23）。此前每个工作区**只有一个可用的人**：同事登录后 `roles: []`、八个能力分区全隐藏、渲染锁定态。含**最后一名管理员不可移除**守卫（所有者引导只在首次登录跑，无人持有 `admin.manage` 即无回头路），以及 `lockoutReason()` 区分「未订阅」与「无角色」——后者付再多钱也解决不了 |
| 3j | 决策链补录关系 | 已交付（PR #27）。`linkContacts` 此前无调用方：面板告诉销售「决策人不可达」却不提供任何能改变它的动作。测试断言的是**判断翻转**，不是写入成功 |
| 3k | 计划与战役生命周期控件 | 已交付（PR #28）。`transitionPlan` / `transitionCampaign` 是最后两个无调用方的服务函数。选择器只列迁移表允许的动作，并以**等价关系**断言（每一组 `(from,to)`，「提供」与「接受」必须同真同假） |

### 一轮六视角审计（2026-08-16）

对全仓做了一次六视角审计（产品完整性 / 门控正确性 / 数据完整性 / 智能体两平面 /
测试缺口 / 界面质量），36 条原始发现，经**独立质疑者对抗验证**后确认 4 条 critical，
4 条被驳回（这正是验证阶段的价值——半数没扛住敌意复读）。四条已全部关闭：

| 发现 | 处置 |
|------|------|
| D6 管道完全只读，无人能推进/定价/关闭商机 | PR #22 |
| 无角色管理界面，工作区只能有一个可用的人 | PR #23 |
| 放弃 SSE 流只 `releaseLock()` 不取消，上游继续生成并计费 | PR #24 |
| `platform/s2s.ts` 零测试，ADR-004 的计费主体不变量无人守 | PR #25 |

PR #24 顺带发现超时其实什么都没管住：`timeoutMs` 在响应 resolve 时清除，而那是
**响应头到达**的时刻，两个客户端的 body 读取都是无界的。PR #25 在写测试的过程中发现
`cacheKey()` 用 `"|"` 拼接**不是单射的**（`tenant "a|b"+ws "c"` 与 `tenant "a"+ws "b|c"`
同键），一个工作区铸出的凭证会被交给另一个——测试第一次跑就红。

PR #26 处理审计尾部中**不涉及商业判断**的部分：账户详情页直接持有 store 句柄（隐藏
导航链接不是访问控制，直接输 URL 照样返回客户档案与联系人名单）；`execute()` 在门之前
读行，使未授权者能通过 `not_found` 与 `permission_denied` 的差异**枚举有效提案 id**。

### 对同一轮自身产出的复审（PR #31）

上述十个 PR 是在一次连续作业里写的，只有作者本人看过。于是把同一套机器（五视角 +
独立质疑者）对准 `e65a7ea..HEAD` 这个范围本身：27 条原始发现，**4 条确认、4 条驳回，
四条确认的全部是这一轮新引入的**。

| 缺陷 | 性质 |
|------|------|
| `planStageChange` 不移动 `forecast_category` | 赢单后仍留在 `commit` 桶，`rollUp()` 把**已赢收入算成未到手承诺**，`closedAmount` 为空——同一笔钱报两次。且该行处于 `planCategoryChange` 双向拒绝的状态，后续任何编辑都修不回来 |
| 预测类别写在编辑门之下 | 类别属 `pipeline.forecast`（pro + 专有权限，规格文档明写），却被折进 `pipeline.opportunity.update`（免费档）。反向荒谬：规格指定的 `sales_ops` 反而调不到该函数 |
| 已关闭商机保存必失败 | 上一条的后果，表单每次提交全部字段，把过期的桶带回去 |
| 拒绝的流丢失错误信封 | **修 #24 泄漏时引入**：`abort()` 放在读 body 之前，`GRANT_DENIED`/`QUOTA_EXCEEDED`/`RATE_LIMITED` 全塌缩成裸 `"403"` |

**这一轮的方法论结论，值得记住：**

- **审计擅长发现「没接上的东西」，不擅长发现「接错了的东西」。** 四条 critical 都是
  「后端完整但界面没接」，而 #31 的四条全是「接了但接错」——后者要靠动手复算、以及
  让测试去撞才会暴露。
- **一个测试的价值取决于它断言的是什么，不是它有多少行。** 往返测试断言了收关后的
  状态、赢率、`closed_at`、金额，唯独漏了类别，于是一路绿着放行了最严重的一条。
- **修复本身会制造缺陷。** #24 修泄漏时弄坏了诊断能力，而 #24 自带的测试因为假 body
  没接到 `init.signal` 上而抓不到。新测试留下之前先把修复回滚验证它确实变红——
  **一个不会为它守护的 bug 变红的测试，不是测试**。

### 界面层的两条纪律

**没有任何页面在本地重算商业结论。** 按钮的可见性一律来自服务端动作会重跑的同一个
`can()` 门。信号页曾经用 `["pro","business","enterprise"].includes(tier)` 算过一次，
上线前被抓掉——那是产品在本地推导商业结论，套餐一改就漂移。

**全部非 ASCII 文本集中在两个纯数据文件**：`(app)/lib/messages.ts`（界面文案）与
`domains/shared/demo-fixtures.ts`（演示数据的展示文本）。两者都不含逻辑，`app/` 下
其余源文件全部 ASCII，可机器穷举（TD-002）。

## 已交付的运行形态（批次 2-3 完成后）

```
浏览器 ─ (app)/ 八个能力分区页面 + 会话/裁决
           │  服务端组件解析会话 → 权益 → 权限，客户端只拿结论
           ↓
        domains/<d>/service.ts    两道门 → 规则纯函数 → 端口
           ↓
        domains/<d>/store.ts      端口（workspace_id 是每个方法第一个参数）
           ↓                      内存实现 | Prisma 适配器（写前过列锁镜像）
        PostgreSQL 34 张表        列级写锁兜底

        agent/atlas   模型面（唯一 LLM 出口，只按 endpointCode 路由）
        agent/runos   能力面（MCP 四工具，Skill 只分发不执行）
```

**609 项单测**；`quality-gate` / `build` / `test-coverage` / `audit` / `gitleaks`
五个必需检查全绿。

## 批次 3.5 - 证据面与两条平台面（PR #36-#46）

批次 3 之后追加的一轮，起因是 owner 的重新定位：**把原来散在各销售人员脑子里的信息
借助智能体沉淀到平台，再基于历史信息做分析、形成新信息，去指导和督促销售**。传统系统
记录信息，这个产品要在记录之上产出**新**信息——那么第一件事就是：得先真的有记录。

### 证据面（ADR-006，PR #38-#43）

| PR | 交付 |
|----|------|
| #38 | `check-incr-grants.mjs`：增量建表必须自带授权，否则守卫红 |
| #40 | `incr/0004`：`yucer_field` 三张表（interaction / interaction_participant / commitment），**自带授权与列锁**——db-init 先跑 97/98 再跑 incr，写在 98 里会对着不存在的表执行 |
| #41 | 规则与服务：**承诺不能靠断言关闭**。`met` 必须指向一次真实交互，数据库 `chk_commitment_met_needs_evidence` 与规则层双重把守；`missed` 什么都不需要——成功要有证据，失败是「什么都没发生」时自然的结果。同时 `lastInteractionAt` 从「阶段移动」这个代理量改为真实接触日期 |
| #42 | 界面：采集表单（**只有一个必填字段**）、逐字时间线、承诺清单（`met` 在选中证据前禁用）、双向逾期队列 |
| #43 | **接线**——`relationshipEvidence` 此前零非测试调用方；采集只在客户页而判据按商机计；**判据本身没有任何东西在测**。三个断点全部补上，新增 `/admin/adoption` |
| #46 | 决策链接入交互参与人；逾期承诺扫描作业 |

**一期带杀死判据**：六周窗口，按 `/admin/adoption` 的覆盖率读数决定二期（主张与判断层）
建不建。判据的两个决定值得记住：

- **测覆盖率，不测数量。** 一个人往一条商机上倒二十条笔记，「每周交互数」会很好看，
  而另外十九条商机全黑——由此得出的「习惯形成了」在唯一要紧的意义上是假的。
- **刻意不按人拆分，并写在界面上。** 一旦能当绩效看，大家就会为它记录，它就不再测量
  它要测的东西。

窗口未满一律返回「观察期未满」而非失败：**一个能提前失败的判据，一定会被提前引用。**

### 两条平台面（PR #45）

owner 指示接入 karda（知识）与 arda（共享数据）。两者拿到**相反的答案**，
见 [ADR-007](../30-design/decisions/ADR-007-karda-as-the-only-knowledge-crossing.md)
与 [ADR-011](../30-design/decisions/ADR-011-arda-on-the-signal-seam.md)，
首封联络函见 `docs/80-liaison/01-2608161131-...`（16 问）。

### 这一轮的方法论增量

批次 3 记过一条：**审计擅长发现「没接上的东西」，不擅长发现「接错了的东西」**。这一轮
补两条：

- **「后端完整、界面没接」会重复发生，而且发生在自己刚写完的代码里。** #43 修的三条
  全部来自 #40-#42——同一个人、同一周、同一个功能。因此「有没有非测试调用方」值得
  作为收尾的固定检查项，而不是靠记性。
- **可以把「线格式的猜测」隔离在适配器里；不可以把「不属于自己的词汇表」隔离起来。**
  见下节被丢弃的 karda 接入。

## 批次 6 - 把已建成的后端接上界面（2026-08-26 规划）

这一批**不新建能力**。它偿还的是本仓已经记过三次、这次第四次复发的同一类债：
**后端完整、测试全绿、权限已定义、表已建，界面接不到**。

排序依据不是难易，是**哪一条债正在让真实工作区的数字算不出来**。

### 6a 钱的链路（最高优先）

四个服务函数零界面写入路径，串起来正好是 `定目标 → 报预测 → 交付 → 回款`：

| 项 | 函数 | 现状 | 验收 |
|----|------|------|------|
| 6a-1 | `submitForecast` | **已交付** | 见下 |
| 6a-2 | `createTarget` / `updateTarget` | **已交付** | 见下 |
| 6a-3a | `reconcileProjectHealth` | **已交付** | 见下 |
| 6a-3b | `transitionInstalment` | **已交付** —— 回款计划面板 | 见下 |
| 6a-4 | 收尾守卫 | **已交付（提前）** | 见下 |

**先做 6a-1**：它是四条里唯一连「未接线」注释都没有的，说明没人注意到它断了。
后果最重：`forecast_snapshot` 空 → 预测轨迹永远空、`attainment()` 无分子，
而 `attainment()` 有八个调用方。

#### 6a-1 已交付（2026-08-26）

`pipeline/forecast-action.ts` + `components/submit-forecast.tsx`，控件落在**轨迹区**
而不是上方总额板：总额板是「现在是多少」，每次加载重算；快照是**冻结下来供日后争论的一次读数**。
按钮放在总额旁边会暗示它保存那个数字，放在序列上说的才是它做的事——给序列加一个点。

作用域只做 `workspace`：另外两个（territory / owner）需要选择器，而选择器不存在——
提供一个只能产出一个值的下拉，是一个对自己撒谎的控件。

**实测验收**：轨迹末点从种子数据的 08-01 变成当天；连点两次，点数 14 → 15，
两行同日——**追加而非改写**，与 `forecast_snapshot` 的 UPDATE 已 REVOKE 一致。

#### 6a-4 已交付（提前到 6a-1 一起做）

`domains/shared/wired.test.ts`：遍历每个 `service.ts` 的导出动词，断言它在本域文件夹
之外有非测试调用方。未接线是允许的，但**必须具名**，写明原因与偿还它的批次项——
把「我们忘了」变成「我们决定了，并且这是它结束的时间」。

写的时候它先绿了一次假的：第一版用裸词匹配，把 `delivery-table.tsx` 注释里那句
「neither is wired」也算成了调用——**解释「没接线」的注释，被当成了接线**。
去注释后再验：拆掉 `forecast-action.ts` 的调用，守卫变红；恢复，变绿。
**一个会被注释骗过的守卫不是守卫。**

它证明不了的事也写在文件里：它证明「域外有东西调用了这个动词」，
不证明「人能触达它」。四次历史缺陷全部是连 action 文件都没有，所以它挡得住复发的那个形状。

#### 6a-2 已交付（2026-08-26）

`planning/actions.ts` + `components/set-target.tsx` + 表格行内菜单。

**两种手势，分开做**，而且表格自己早就论证过：建立目标是**表单**——它需要周期、作用域、
指标、金额，这四样在有人敲进去之前都不存在；调整目标是**行内动作**——数字就在屏幕上，
你改的是那个数字。三点菜单是后者的正确入口，是前者的错误入口。

表单放在表格**上方**：新工作区里表格必然是空的，把建立入口塞在一张没人能填满的列表
下面，等于把门放在锁着的门后面。

**DS 逼出了一个有用的问题。** `ActionMenuItem` 只允许 `confirm` 出现在
`danger: true` 的项上——要确认框，就得承认这个动作是危险的。于是：

- **提交为承诺** = Plain + hint。它单向不可逆，但它是**正常步骤**，是这个页面存在的
  意义。把主路径染红，会花掉「关闭」真正需要的那点颜色；单向这件事由规则层强制。
- **关闭本期** = `danger: true` + 确认框。它冻结一个已结束周期上承诺过什么，
  而改它正是「把没达成的季度改成达成」那条路。

已关闭的目标**没有菜单**，不是灰掉的菜单：规则层拒绝它的每一个补丁，
灰项只会邀请一次注定失败的点击。

**实测**：建了一条「全工作区 / 新客户数 / ¥3,000,000」,落为草稿；草稿行三项齐全，
已承诺行只剩两项（提交为承诺正确消失）。

**顺带修的两个文案 bug（我自己的）**：为了复用现有键，把 `columnScope`（列头「作用域」）
当成了区域名，把 `ownerScope`（`(sub) => sub`）当成了标签而渲染出空选项。
**因为一个词看着对就借它的键，是标签描述错东西的典型路径。**

**留下的问题，未处理**：`new_logo`（新客户数）是一个计数指标，
而 `SalesTarget.targetAmount` 的类型是 `Money`——表单于是让人用货币填一个数量。
这是既有的建模缺口，不是本次引入，登记在此不静默。

#### 6a-3a 已交付（2026-08-26）

`delivery/actions.ts` + 项目表行内菜单「重算健康度」。

**三种结局，三句话**，不合并成一句「已重算」——那会盖掉最要紧的那一种：
填报与推导**本来就一致**，这跟「刚纠正了一次误报」是两回事。

| 结局 | 文案 | 语气 |
|------|------|------|
| 改动了 | 已改为 X + 原因 | warning |
| 一致 | 填报与推导一致，未改动 | info |
| 拒绝 | 门给出的原话 | danger |

**实测**：演示数据里 PRJ-0001 是「填报绿灯却带一笔逾期分期」,表格显示
「关注 · **已下调**」。跑一次重算——`已下调` 消失，行仍是「关注」：
推导值被写进存储，填报与推导达成一致。再跑一次，提示变成
**「填报与推导一致，未改动」**——三结局的区分在实机上成立。

#### 6a-3b 重新定级（2026-08-26）

规划这一批时我把它和 `reconcileProjectHealth` 归成同一件事——「`/delivery` 没有
action 文件」。查下去发现不是：`projectView` 返回 `instalments`，
**但没有任何地方渲染它们**，也没有 `/delivery/[id]`。

所以 `transitionInstalment` 缺的不是一条 server action，是**一个界面**——
它就是功能域面板里那个标着「开发中」的**回款计划**模块。这属于新建，
不属于本批的还债，移到 6b 之后按新建排。

**这次记的是：盘点时把「没接线」和「没界面」看成同一类，会低估工作量。
两者的证据不同——前者看 grep 得到的调用方，后者要看渲染树。**

### 6b D9 产品目录（门已设计好，缺实现）

ADR-017 已定 D9 分区、六条 `feature: null` 的 action、三个权限，`incr/0010` 已落。
`domains/catalog/` 有 store、lib、Prisma 适配器，**缺 `service.ts`（两道门）和页面**。

| 项 | 范围 |
|----|------|
| 6b-1a | **已交付** —— `service.ts` 的读半边：三条 view + 一条行项读 |
| 6b-1b | **已交付** —— 写半边：规则、端口、内存实现、Prisma 适配器 |
| 6b-2 | **已交付** —— `/catalog` 一页三段，归入战略武备域 |
| 6b-3 | **已交付** —— 商机行项编辑，写行项的同一次调用重算 header |
| 6b-4 | **已交付** —— 折扣签字：底价第一次有人能越过（ADR-019） |

#### 6b-1a 已交付（2026-08-26）

`domains/catalog/service.ts`,四个读函数配 5 条测试。

**门是纯权限的，这是分区的设计而非省略。** `catalog.*` 的 action 全部
`feature: null`——目录是链路基础设施不是可售卖能力（ADR-017）。所以每个函数照样跑
两道门，只是权益那半边每次都判为 baseline。**把 `can()` 省掉会得到一个日后谁也
不敢加功能键的服务**——因为没人知道要审多少个调用点。

测试断言的正是这件事：**五个档位逐个跑一遍**。反证过——给
`catalog.product.view` 塞一个功能键，跨档测试立刻变红。
拒绝路径用手工构造的空权限持有者，因为七个角色**全部**持有 `catalog.read`（这是
预期状态），否则那条路径永远不会被测到。

**顺带修掉一个网关绕过。** 接线守卫报 `catalog.listProducts` 已被调用——查下去是
`/pipeline` 和战况板**直接持有 store 句柄**读目录，两道门全跳过。
这和 PR #26 在账户详情页修的是同一类缺陷。四处直读全部改走服务；
`grep getCatalogStore()\.` 在 `domains/` 之外now 为空。

顺带补了 `listOpportunityLines`——它**挂 `pipeline.view` 而不是目录的 action**：
`opportunity_line` 住在 `yucer_pipeline`,按 ADR-001 归属 D6，行项是**交易的一部分**，
能不能读交易才是它的准入条件。

#### 6b-1b 已交付（2026-08-26）

规则（`lib/pricing.ts` 追加三个 plan 函数）、端口写方法、内存实现、
**`prisma-store.ts`——本域的第一个适配器**、三条写服务、5 条新测试。

**权限分离是这一项的核心，也是唯一被反证过的断言。**
`catalog.write` 维护卖什么，`catalog.price` 决定不再往下让的那条线。
测试用手工构造的持有者把两者**分别**给出去，断言各自**做不到对方的事**——
把 `catalog.pricebook.upsert` 的权限改回 `catalog.write`,这条测试立刻变红。
一个编目录的人顺手就能移动底价的产品，是一个每笔折扣都自我批准的产品。

**价格是追加的，不是就地改的。** `effective_at` 属于唯一键，新时点的新价格是新行——
这正是「价目**书**」区别于「价目」的地方：被取代的那一行仍可读，它解释了今天这个
数字是怎么来的。`incr/0010` 给 list/floor 的 UPDATE 授权是为了改刚写错的那一行，
不是为了改写历史，端口刻意不暴露它。

**三条规则各自挡住一件事**：产品必须有**单位**（没有单位的「10 × 1000」是十个坐席、
十天还是十个站点，那是三笔不同的生意）；方案必须**装着东西**（空方案是个名字）；
底价**不得高于标价**（那会让每一笔都需要签字，等于没有底价）——
但底价**等于**标价是允许的，「此产品不打折」是一个立场，不是笔误。

**适配器的两处事务不是保守**：方案的 items 是**整体替换**,delete 与 insert 之间失败
会留下一个空方案，而空方案正是规则层拒绝任何人创建的东西。

#### 6b-3 已交付（2026-08-26）

`replaceOpportunityLines` 落在 **pipeline** 而不是 catalog：行项住在
`yucer_pipeline`,按 ADR-001 归 D6——定一笔交易的价属于
`pipeline.opportunity.update`,目录只是它读的维度。

**ADR-014 §2 就是这个函数的全部**：行项存在时行项是权威，
`opportunity.amount` 等于它们之和。这条约束写在服务层而不是 DDL——
它跨行（一个 header 对多条 line）,CHECK 表达不了，触发器会把业务规则藏进数据库。
于是服务在**写行项的同一次调用里**重算 header，而且这是**唯一**允许改 amount 的路径。

**`needsApproval` 由服务算，永不接受调用方传入。** 客户端能设的标志就是客户端能清的
标志，而这一个决定一笔折扣要不要送到人面前。逐行按「该产品该币种当时生效的那一条」
定价——拿一次查询给整批定价，会让一条过期底价去裁定它从未适用过的产品。

**实测**：把一行数量 16 → 8，运行合计在提交前就显示 1,900,000；保存后
详情页与管道列表页的 header 双双变成 ¥1,900,000。
再把一行单价压到 1，服务端标出「低于底价，需要签字」;恢复后标记消失。

**六条测试守着这条规则**，其中最要紧的一条断言的是**存储里的 header**而不是返回值——
一个报告了自己没写进去的总额的函数，会在它自己的输出上通过测试，
而把两个数字留在那里各自漂移。反证过：让服务写 `total + 1`,这条立刻变红。

#### 6b-4 已交付（2026-08-28）

6b-3 交付时留下了一个自己造出来的死结：定价规则会把低于底价的行项标成待批，
战况板会计数，徽标会显示，**而权限目录里没有任何一条权限能把它降下来**。
`pricing.discount_approval` 只是 ADR-015 的一个能力标签，说的是智能体可以*提议*，
不是有人可以*做出*。底价创造了一个没人能满足的签字要求。

**新权限 `pipeline.discount`（incr/0012），与 `pipeline.write` 分离。** 报出低于
底价的人不能是给它签字的人。授予范围与 `catalog.price` 相同——定底价和为底价开例
外是同一份权力的两半，而 sales_ops 本来就能移动底价。功能键仍是 19。

**签字按价格记，不挂在行项上。** 行项是整体替换写入的，挂在行项上的签字会被同一
商机上任何另一行的编辑销毁。键取
`(opportunity_id, product_id, unit_price, currency)`,于是三个性质是白送的：改别的
行签字仍在，重新报得更低就匹配不上（对，没人签过新数字）,改回签过的价格又匹配上
（也对）。追加表，无 UPDATE / DELETE 授权。

**实测**：sales_rep 把单价压到 700（底价 800）,行项列「折扣」显示「待批」且**没有
批准按钮**；同一页以 sales_leader 打开，按钮出现，填理由签字后该行变「已批准」,
管道列表的待批徽标从 1 归零。改到 600 后徽标重新出现，改回 700 又消失。

**九条测试**，其中两条做过反证：把 `approvalFor` 的价格比较去掉，「改低即失效」立刻
变红；把 action 的权限改成 `pipeline.write`,「销售不能给自己签字」立刻变红。

**顺带修好一个守卫的盲区。** `column-locks.test.ts` 原来只从「先全授权再 REVOKE
UPDATE」推导追加表。本增量的表用的是更紧的写法——建表时就只授 SELECT / INSERT——
于是对那条推导完全不可见。推导已拓宽为认两种写法，并配了各自的非空断言。
（一度以为它顺手抓到了 `yucer_field.commitment` 的缺失授权，查证是我漏看了它后面的
列级 `GRANT UPDATE`,那里没有缺陷。）

### 6c 战略客户写入路径

比看上去建得深：`incr/0006` 有 `account.tier` 与 `account_plan`，端口有
`getAccountPlan`，判断引擎的节奏规则在跑，商机详情页读 tier 分三档。
**缺的只是没人能把客户标成战略客户，也没人能写它的计划**——与 6a 同一个形状。

### 6d 功能域面板的诚实性（小）

面板把**赢丢复盘**和**回款计划**标成「开发中」，两者都已建成——前者渲染在
`/pipeline`，后者在 `/delivery` 的表里。根因是把「没有独立路由」当成了「没建」。
修法不是改标签，是给 `DomainModule` 加第三种形态 `section`（已建、活在别的页面里，
点击跳转并定位），并让 `functional-domains.test.ts` 断言
**一个已渲染的组件不得被标成 planned**。

### 不在这一批

- **批次 4 / 5 不变**，卡在平台侧（OIDC 注册、arda 契约），不是本仓能推的
- **问参谋** 仍是 disabled，等 agent plane
- **`帮助` 按钮指向哪 / `通知` 的数据源** —— 两个从未回答的产品问题，等 owner

## 批次 4 - 平台注册与首次上线

| 项 | 范围 |
|----|------|
| 4a | 平台侧注册：OIDC client `yucer` / `yucer-beta`、域名 `yucer.vxture.com`、端口分配 |
| 4b | `PRODUCT_CODE` 仓库变量、部署 Environment 与密钥、worker02 栈根目录 |
| 4c | `db-init` 应用三段式 DDL + `incr/0001` 种子 |
| 4d | 首个生产 tag `v0.1.0` |

清单见 `docs/50-deployment/10-platform-registration-checklist.md` 与
`20-github-bootstrap-checklist.md`。

## 批次 5 - 商机侦探数据接入

| 项 | 范围 |
|----|------|
| 5a | 信号采集接入（`signal.external_feed`，business 档位能力） |
| 5b | 信号评分模型与衰减策略（口径见 `20-specs/30` 第 3 节） |
| 5c | 线索转商机的归因闭环验证 |

## 等待外部裁定的事项

| 事项 | 卡在哪里 | 记录 |
|------|---------|------|
| **TD-002** 界面文案违反 source ASCII-only | 等**平台仓修订标准**。`CLAUDE.md` 规定标准缺口先在平台仓修、不得在产品仓自造，因此本仓只登记不裁定 | `docs/60-operations/00-index.md` |
| **karda 接入** | 等**平台发布 karda 契约**。本仓已落 [ADR-007](../30-design/decisions/ADR-007-karda-as-the-only-knowledge-crossing.md)，把「消费侧立场」定死（不自建第二个知识存储），但**契约发布前零代码**——2026-08-15 写过的一版已整体丢弃，见下 | [karda#103](https://github.com/vxture/vxture-karda/issues/103) |
| **arda 能跑** | 等 base URL / audience / scope / 游标语义。一期**可合并、可单测，跑不起来**，与 Atlas / Runos 今天状态相同 | [arda#212](https://github.com/vxture/vxture-arda/issues/212) |

**其余三项已由 owner 于 2026-08-16 裁定并落地**，此表不再持有它们：

| 事项 | 裁定 | 落地 |
|------|------|------|
| 提案队列免费档可读 | **维持免费,但必须命名** | PR #33：新增 `copilot.action.view`，有效门不变。免费档队列必然为空（pro 以下产生不出提案），唯一能读到内容的是**降级后**的工作区，那正是挽留面本身 |
| `strategy.plan.approve` 成为真权限 | **做,只发 `sales_leader`** | PR #33 + `incr/0002`。`marketing_manager` 保留 `strategy.write`，可起草修改，但不能替销售组织签字。目录 19/67 → 20/68 |
| Prisma 适配器完整性 | **建 Postgres 18 CI 通道** | PR #34（通道 + `incr/0003`）、PR #35（三个适配器缺陷）|

### 数据库通道（PR #34/#35）

`db-contract` job：`postgres:18` service 容器，按 `db-init.yml` 的**同一顺序**跑真
DDL，然后跑 `*.db.test.ts`（15 个）。**未加入必需检查集合**——那会改动分支保护
ruleset，是 owner 决定。

之所以必须先建通道再修适配器：唯一索引、CHECK、REVOKE 和 NULL 比较都是**数据库的
性质**，内存适配器建模不了。有几个缺陷就是这样在 788 个绿灯下活下来的：

- `uidx_sales_target_scope` / `uidx_forecast_snapshot_scope_at` 建在可空列上，而
  Postgres 的 UNIQUE **把 NULL 视为互不相同**——所以这两个约束**对工作区级行完全
  失效**，而那正是其他所有数字的基准。`incr/0003` 用 `NULLS NOT DISTINCT` 重建。
- `count()` 分配编号的三处注释都声称"放在事务里所以安全"。事务给的是**原子性不是
  串行化**：READ COMMITTED 下两个并发事务读到同一个 count。改用
  `pg_advisory_xact_lock`，按分配种类和作用域双重分键。
- `saveWinLossReview` 的 upsert `where` 未按工作区限定；`recordSignal` 的裸 `catch`
  把外键/CHECK 失败一律报成"已存在"。

**通道在它自己第一次运行里就抓到了三个我写的缺陷**：CHECK 测试的第 2、3 条断言因
Postgres 事务中止语义而实际无效（约束删掉照样绿）、硬编码容器口令、安装未禁用生命周期
脚本。这三件都只有对着真数据库和真 CI 跑过才会现形。

关于审计的一处措辞更正：它称 `deletedAt` 过滤意味着"全仓服务层断言证明的是另一个
实现"。**这说过头了**——没有任何产品路径能设置 `deleted_at`（所有 port 都没有删除
方法），两个适配器返回的是同一批活行。真正成立的是**排序**分歧（PR #30）。

TD-002 的现状是**收敛而非解决**：全部非 ASCII 文本集中在两个纯数据文件——
`portals/app/app/(app)/lib/messages.ts`（界面文案）与
`portals/app/app/domains/shared/demo-fixtures.ts`（演示数据展示文本），其余源文件
全部可机器校验。**收敛的口径是「可穷举且只含数据」，不是「只有一个文件」**：新增中文
文本只能进入这两个文件，装配逻辑（如 `demo-seed.ts`）必须保持 ASCII。

标准修订后二选一收口——平台仓为终端用户文案开显式口子，或引入正式 i18n 方案把文案
移出源码树。**在裁定之前收敛状态维持不变，不要「顺手修掉」**：把文案改成 `\uXXXX`
转义确实能满足字面规则，但会得到一份人类无法维护的文案和一份无法评审的演示数据。

### 一版被丢弃的 karda 接入（2026-08-15）

本仓曾写过 `platform/karda/{types,redact,client}.ts` 三个文件，含一个「主张可外发、
证据不可外发」的边界模块。**已整体丢弃**，不是风格问题——一次独立对抗审查（四视角 +
独立质疑者，62 个 agent、28 条确认发现）在那个 137 行的边界文件里查出**四条各自独立
的客户名称越界路径**：最强的一道检查 `knownNames` 通过端口结构上不可达；`derivedFrom`
原样外发且其唯一校验器是死代码、还按 `acc_` 前缀写而本仓主键全是 `gen_random_uuid()`；
facet 的**键**从不检查；正则是照测试夹具而非真实中文文案调的。外加该模块**从未编译
通过**（全仓唯一一处 tsc 错误，硬失败 `build`）。

**方法论上的结论，比这些 bug 本身重要：**

> **可以把「线格式的猜测」隔离在一个适配器里；不可以把「不属于自己的词汇表」隔离起来。**

那版代码自造了 `KNOWLEDGE_KINDS` / `KNOWLEDGE_SCOPES` / `KnowledgeClaim` —— 一个本仓
并不拥有的领域的词汇表 —— 再把一条**不可逆**的披露边界挂上去。猜错时端口错、边界规则错、
所有调用点全错，传输层隔离救不回来。arda 一期之所以能现在就写，正是因为它的端口返回
**yucer 自有的 `NewSignal`**，唯一的猜测被关在 `http-source.ts` 一个文件里。

## 未决事项

- ~~智能体的模型编排与工具协议尚未定型~~ → 已由
  [ADR-004](../30-design/decisions/ADR-004-atlas-and-runos-as-the-only-agent-planes.md)
  决策：模型走 Atlas（只按 `endpointCode` 路由），能力与技能走 Runos 四工具面，
  Skill 只分发不执行。
- Runos 的 `delegation_token`（对象级判定凭据）如何从 yucer 会话派生，未定型；当前
  实现只透传，不自行构造。
- ~~信号外部数据源的选型与合规边界未定~~ → owner 于 2026-08-16 指示对接 **arda**
  （平台共享数据面），由
  [ADR-011](../30-design/decisions/ADR-011-arda-on-the-signal-seam.md) 落地：arda 是
  **入站事实来源**，接在信号缝上，一期只取数、不外发、**零 DDL**，带 90 天杀死判据。
  `signal.external_feed` 这个已按 business 档位售卖但后面空着的 feature key，第一次
  有了实现。合规边界（留存 / 删除 / 撤回）仍未定，是联络函第 16 问，也正是一期不做
  外发的理由。
- **接入前置（运营侧，不在本仓）**：Atlas 需要一条 `(yucer, endpointCode)`
  product-grant，否则任何模型调用都是 `403 GRANT_DENIED`；Runos 生产目录当前为空且
  enforcement 关闭，首注册 / 首授权 / 开 enforcement 是三步运营动作。在那之前
  `runos_discover` 返回空列表是**正常返回**，不是故障。

## 批次 6 收官（2026-08-26 夜）

**6a / 6b / 6c / 6d 全部交付。** 接线守卫的允许清单从五条降到**一条**，
而那一条是有裁定的：

```
signal.previewAttribution  → 无设计好的界面，见 ADR-016
```

### 各项落点

| 项 | 交付物 |
|----|--------|
| 6a-1 | `/pipeline` 预测快照按钮；轨迹从种子末点长出当天的点，连点两次得两行 |
| 6a-2 | `/planning` 建立目标表单 + 行内调整/提交/关闭 |
| 6a-3a | `/delivery` 重算健康度，三种结局三句话 |
| 6a-3b | `/delivery` **回款计划面板**：菜单只出迁移表允许的动作，终态无菜单，已回款必须填实收 |
| 6a-4 | `wired.test.ts` —— 每个域动词必须有域外调用方，未接线必须具名并写明偿还位置 |
| 6b-1a | `catalog/service.ts` 读半边 + 修掉四处网关绕过 |
| 6b-1b | 写半边 + 本域第一个 Prisma 适配器 |
| 6b-2 | `/catalog` 一页三段（产品 / 方案 / 价目），D9 进入导航 |
| 6c | `/account/[id]` 定级控件：战略客户必须配计划，规则层强制 |
| 6d | `DomainModule` 第三形态 `section`,修掉「把已建成的说成开发中」 |

### 这一夜记下的几条

**一个会被注释骗过的守卫不是守卫。** `wired.test.ts` 第一版用裸词匹配，
把 `delivery-table.tsx` 里那句「neither is wired」当成了接线证据。

**两面互为镜子的东西彼此吻合，说不出它们中任何一个是不是真的。**
列锁的双向对账让我写的 `GRANT UPDATE (..., status, updated_at)` 一路绿灯——
而 `price_book_entry` 根本没有这两列，db-init 会在部署时炸。补了第三个参照点：
`CREATE TABLE` 语句本身。这条新守卫自己第一版还犯了它要防的错
（`Object.entries` 用在 Map 上，循环没跑，静默通过），是反证救的。

**一个误报的守卫比没有守卫更贵。** 同一条守卫第二次运行把真实存在的
`account.tier` 报成缺失——懒惰的 `[\s\S]*?` 跨过了分号，把别的文件的
`ADD COLUMN` 记到了错的表名下。按 `;` 切成语句才对。

**盘点时把「没接线」和「没界面」看成同一类，会低估工作量。**
两者的证据不同：前者看 grep 得到的调用方，后者要看渲染树。

**TD-010 会在新代码里复发。** 目录的第一版把规则层的英文违规串直接渲染进了中文界面
（"a floor above list price would make every sale need approval"）。
现在三处写路径全部返回**违规码**,句子归字典。

### 收尾复核（2026-08-27）

把批次 6 声称交付的每一项都对着仓库查了一遍。**功能全部在**：四个 action 文件、
`/catalog`、回款计划面板、定级控件、`DomainModule` 第三形态都在 `main` 上，
1030 条单测 0 失败（27 条是 `*.db.test.ts` 无 `DATABASE_URL` 的自跳过，走 CI 的
`db-contract` 通道）、四条 guardrail 与 `tsc --noEmit` 全绿、生产构建出 17 条路由。

**问题全部出在 6d 自己——那一项恰好就是「诚实性」。**

**一个只匹配自己编出来的名字的守卫，是必然通过的守卫。** `functional-domains.test.ts`
的诚实性检查按 `key -> 组件名` 匹配，十二个名字里**十一个在仓库中不存在**
（`InstalmentList` 实为 `CollectionsPanel`,`ProductTable` 实为 `CatalogPanels`……）。
唯一能匹配上的 `PendingReviews` 对应的模块已经是 `section`,循环根本不进。
它不是「暂时没抓到」,是**结构上抓不到任何东西**。修法与列锁那次是同一招：
补第二个参照点——名字分成 `SHIPPED_AS`（断言**必须存在**,改名不能悄悄退休一条检查）
与 `NOT_BUILT`（断言**必须无人渲染**,谁建出来谁就得改标签）。反证过：
把 `collection` 的名字改回 `InstalmentList`,立刻变红。

**6d 把标签改诚实了，却让它指向一个不存在的地方。** `section("winLossReview", …,
"pipeline", "winloss")` 生成 `/pipeline#winloss`,而**全仓没有任何元素带这个 id**——
点进去落在页面上，不滚动到任何地方，对点的人来说和「这功能不存在」读起来一样。
`solutions` / `pricebook` / `collections` 三个锚点都是实的，只有作为 6d 门面的那个是空的。
补了 `id`,并加了新守卫：**每个 section 的锚点必须在某处作为 `id` 存在**。

**`namedAccount` 维持 planned,这是本次唯一的判断而非事实。** 6c 交付的是**写入路径**
（`/account/[id]` 的 `DesignateAccount`）,而这个模块指的是**名册**——我们的战略客户
合起来是谁。名册不存在；也不能标成 `section`,因为 section 继承宿主的导航项，
而这里的宿主是 `/account/[id]`,它不是导航项。**详情页上的一个控件不是一个界面。**
理由写进了 `functional-domains.ts`,免得下一次盘点重新发现一遍。

**一个已经偿清的债，注释还在替它站岗。** `wired.test.ts` 的 `KNOWN_UNWIRED` 里留着
「这两个等目录页面 —— 见 6b-2」,而 6b-2 已交付、那两条早已不在表里。已删。

**登记：`new_logo` 用 `Money` 装计数。** 6a-2 当时写在工作计划里「不静默」,但
`CLAUDE.md` 规定技术债登记在 `docs/60-operations/`。补为 **TD-013**
（TD-012 已被 PR #63 占用）。

**文档结构：** `#### 6b-3 已交付` 原本被追加在 `## 未决事项` 底下，离它在 6b 表里的
那一行 180 行远。已移回 6b 块内。

### 未合并 PR 清空（2026-08-28）

批次 6 收尾后把 6 条开着的 PR 清了，**开启数归零**。

| PR | 处置 | 依据 |
|----|------|------|
| #64 | 合并 | 本次收尾复核 |
| #63 | 合并 | TD-012，rebase 后与 TD-013 各自入表 |
| #58-#61 | 合并 | 见下 |
| #56 | **关闭（已被取代）** | 见下 |

**四条 Dependabot PR 红了五天，红的原因不是它们自己。** #58-#61 开于 08-22，
`audit` 全部失败，逐字都是 `GHSA-ggr8-5vv4-36mx / deepmerge-ts@7.1.5`——即 TD-012
那条公告。#62 把 override 带进主干之后，四条只需 rebase 就全绿。
**红叉挂在它们身上，病在主干上**，这正是 TD-012 补记的那个误读。

**#56 不是「还没合」,是「已经合过了」。** 它开于 08-17，27 个提交；其产物
（`incr/0005`-`0009`、ADR-013 至 ADR-016）在主干上**逐字节相同**,由 #62 带入。
而 `git diff main pr56` 是 **174 文件 / +2117 / -19348**——那 19,348 行删除是分叉之后
主干长出来的全部东西：整个目录域、`wired.test.ts`、`column-locks.test.ts`、
覆盖率规则。**合并它等于回滚十一天。** 它独有的两个文件是更早的稿：
`domain-nav.tsx` 写的还是「八个域的导航」,而分区已经是九个。

**记一条**：一条长期开着的 PR，「未合并」不等于「未交付」。判断依据是**产物在不在
主干上**,不是 PR 的状态字段。这一条差点让 174 个文件按「合并遗留工作」处理。

## 批次 7 - 门后面没有动词的那些（2026-08-28）

批次 6 收官后，剩下的白地都在功能域启动器里具名标着。挑的判据是「后端已建、只差界面」
——本仓栽过五次的那个形状。查下来只有一个符合，而它其实比那更糟。

### 7a 销售区域 —— 已交付（2026-08-28）

`planning.territory.upsert` 从批次 1 起就在 action 目录里，带着 `planning.territory`
（19 个冻结键之一，**PRO 档起售**），而**没有服务动词、没有端口方法、没有界面**。表、
授权、列锁、门全在，只有中间是空的。付费工作区只能读它无法创建的行；区域作用域的目标
需要 `territory_id`，所以区域目标也设不了。

| 层 | 落点 |
|----|------|
| 规则 | `planning/lib/territory.ts` —— `planTerritory`，按代码 upsert（ADR-017 的形状），拒绝不存在的上级与**成环** |
| 服务 | `upsertTerritory`，门是 `planning.territory.upsert`；环检测读**含已停用**的全量，因为停用的上级仍是真实祖先 |
| 端口 | `upsertTerritory` + `listTerritories(ws, { includeRetired })` |
| 界面 | `TerritoryPanel`，**/planning 的一个 section**（`#territories`），不是新路由——宿主必须本身是导航入口，这条也正是「战略客户」仍然 planned 的原因 |

**顺带修掉一处适配器漂移**：`listTerritories` 在 Prisma 里过滤 `status='active'`，内存
实现不过滤。**每个测试看到已停用的行，生产从来看不到**——两个适配器对同一个问题给不同
答案，是会说谎的夹具。现在两边同一契约，管理面板显式要全量。

**实测**：新建 WEST/西部大区 → 出现在花名册**和目标作用域选择器**里（环闭上了）；
把 NORTH 挂到 East China 下 → 生效；再把 EAST 挂到 NORTH 下 → 拒绝，界面显示
「区域不能直接或间接地成为自己的上级」（走字典的中文，不是规则层英文），EAST 那行未被
改动。启动器里「销售区域」从「开发中」按钮变成 `/planning#territories` 的链接。

**两处守卫反证**：把 `id="territories"` 删掉 → 锚点守卫变红；把模块退回 `planned` →
诚实性守卫变红。还有一条**没有响**的反证告诉我测试比它看起来弱：`sales_rep` 根本没有
`planning.read`，所以换成读的门它照样被拒——补了 `viewer`（有读无写）那条才真正把门钉住。

### 7b 普查：还有 10 个同样的门 —— 见 TD-016

单条修完后做了普查：68 个 action 里 14 个从未被 `can()` 求值，其中 4 个是外部阻塞
（批次 5、智能体面），**10 个是同一笔债**。守卫 `app/authz/gated.test.ts` 已就位，四条
断言，其中「写操作必须在服务端把门、不能只在界面上把」的第一版当场误报了
`admin.member.role.assign`——它的门在 `authz/admin.ts`，服务端代码住在别处。判据从「按
目录」改成「按 .tsx / .ts」。

### 7c 直接新建商机 —— 已交付（2026-08-28）

TD-016 的第二笔。`pipeline.opportunity.create` 自批次 1 起在目录里，而
`createOpportunity` 的**唯一**调用方是线索转化接缝——**一笔生意只能从线索里生出来**。

判据不是我定的，是模型自己写下的：`AttributionSource` 有 `self_sourced`，
`resolveAttribution({})` 有一支 basis 是 `"no lead"`，两支都无法到达。

| 层 | 落点 |
|----|------|
| 规则 | `pipeline/lib/opportunity.ts` —— `planNewOpportunity`，校验名称/客户/金额，并**在规则层结算归因** |
| 服务 | `createOpportunity`，门是 `pipeline.opportunity.create`（与 `.update` 分开：创建冻结一段此后不可编辑的归因） |
| 界面 | `NewOpportunity`，/pipeline 看板之上；客户是**选择器不是输入框**；不提供阶段/赢率/预测类别——新单从 qualify 起步，之后归阶段机 |

**没有阶段选择**是有意的：给一个入口让人把商机开在一个它自己的历史解释不了的状态里，
等于制造以后没人能复盘的数据。

**实测**：建「走廊里听来的单子 / 西部能源装备 / ¥800,000 / 2026-09-25」→
`OPP-00001`，合格判定 / 管道 / 10%（阶段默认赢率），落在 Q3 所以进了周期过滤后的看板；
详情页归因显示**「无归因（非战役来源）」**——那条此前不可达的分支。

**反证**：把门换成读 → 服务测试与**上一批建的 `gated.test.ts` 同时变红**（守卫上线一天
就抓到了）；把归因改成不经规则直接写 → 自拓那条变红。

### 7d 联系人可以录入了 —— 已交付（2026-08-28）

TD-016 第三笔，也是这类缺口里最尖锐的一个：**邻居是好的**。`linkContacts` 早就实现并
接了界面，所以成员可以在联系人之间连线，却没有任何路径创建一个联系人。首页头条
「N 决策人未触达」从 `contact.decision_role` 算出——**此前只能描述种子数据**。

| 层 | 落点 |
|----|------|
| 规则 | `account/lib/contact.ts` —— `planContact`，校验角色/状态（两个 CHECK 的镜像）与 0-100 的影响力 |
| 服务 | `upsertContact`，门是 `account.contact.upsert`；id 缺省即新建，带 id 即编辑那一行 |
| 端口 | `upsertContact(ws, accountId, draft)`，返回 null 表示「不在该客户名下」 |
| 界面 | `ContactRoster`，`/account/[id]` 上、**决策链之外** |

**放在决策链之外是关键**：决策链由 `account.graph` 把门（PRO 档），录入联系人骑的是免费
的 `account.manage`。嵌进去会让 starter 工作区一个联系人都记不下——然后它看不到的那个
覆盖率还是从空气里算出来的。

**实测**（北方通信，原本零联系人）：录入「王总 / 副总裁 / 经济决策人 / 影响力 85」后，
决策链从「还没有联系人」变成
`经济决策人不可达 | 已覆盖：经济决策人 | 缺失角色：技术决策人、内线`。

**三条反证**：编辑谓词去掉 accountId → 「跨客户的 id 不该找到」变红；门换成读 →
服务测试与 `gated.test.ts` 同时变红；把 null 影响力默认成 0 → 「null 不等于零」变红。
